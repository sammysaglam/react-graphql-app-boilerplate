type Query {
	currentUserSession: UserSession!
	greeting: String
	sensitiveData: String
}

type Mutation {
	authenticate(email: String!, password: String!): String
	logout: Boolean
}

type UserSession {
	email: String
	isGuest: Boolean
	isAdmin: Boolean
}
